# Node modules
node_modules/

# Build output (if you commit generated files, do not ignore them)
# If you want Vercel to build, keep source files and ignore generated HTML
# But here we commit generated files for simplicity, so only ignore node_modules and logs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env
.env.local
.env.*.local

# OS metadata
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Build scripts – optional to ignore
# build/   (if you don't want to commit build scripts, uncomment)